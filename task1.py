import time
# File should have 1 input that reads an integer
# generates 3 files: t<n>data.py t<n>data.py and t<n>worst.py where x is the integer
# t<n>data.py will have a table of n random numbers from 1 to 10000
# t<n>best.py will have n numbers that are in ascending (sorted) order
# t<n>worst.py will have n numbers that are descending (sorted) order
# for example, the sample data files t10data.py, t10worst.py and t10best.py have been created for you
# each datafile will be importable so we can read the variable that is stored within it. 